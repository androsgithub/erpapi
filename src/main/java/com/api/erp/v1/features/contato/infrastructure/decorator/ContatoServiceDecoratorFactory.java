package com.api.erp.v1.features.contato.infrastructure.decorator;

import lombok.extern.slf4j.Slf4j;

/**
 * Factory legacy - Mantida apenas para referência
 * 
 * A factory atual é ContatoServiceFactory em infrastructure/factory/
 * que segue o padrão de UsuarioServiceFactory e PermissaoServiceFactory.
 * 
 * Todos os beans de ContatoService são criados via ContatoServiceFactory.
 */
@Slf4j
public class ContatoServiceDecoratorFactory {
    // Esta classe é mantida apenas para referência histórica
    // Use ContatoServiceFactory no lugar desta
}
